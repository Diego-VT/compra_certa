name: compra_certa
description: "Aplicativo CompraCerta."
publish_to: 'none'
version: 0.6.0+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  drift: ^2.34.0
  flutter_riverpod: ^2.6.1
  go_router: ^17.3.0
  path: ^1.9.1
  path_provider: ^2.1.6
  sqlite3_flutter_libs: ^0.6.0+eol

dev_dependencies:
  build_runner: ^2.15.0
  drift_dev: ^2.34.1+1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/seeds/categorias_seed_compra_certa.json
